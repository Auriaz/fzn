{% extends 'file_manager/_base.html.twig' %}

{% block dashboard_title %}{{ title }}{% endblock %}

{% block dashboard_adding_btn %}
    <a href="{{ path('file_manager_add')}}" class="dashboard-section__adding-btn btn btn-outline-secondary">
        <i class="fas fa-plus-circle"></i>
    </a>
{% endblock %}

{% block dashboard_body %}
    <article>
        <div class="row">
            <div class="col-sm-4">
                <form action="" method="POST" enctype="multipart/form-data"
                    class="dropzone reference-dropzone"></form>
            </div>
            <div class="col-sm-8">
                {% if manager %}
                    <ul class="list-group small reference-list"
                        data-url="{{ path('admin_article_list_references', { id: manager.id }) }}"></ul>
                
                    <hr>
                {% else %}
                    <p>Brak plików</p>
                {% endif %}
            </div>
        </div>
    </article>
{% endblock %}